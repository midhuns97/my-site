/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://midhuns.com', // Don't include trailing slash
    generateRobotsTxt: true,
    generateIndexSitemap: false, // Optional if you want a single sitemap file
  }
  